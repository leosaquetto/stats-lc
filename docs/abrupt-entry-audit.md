# Abrupt Entry Audit

Registro vivo de pontos em que a UI pode aparecer de forma abrupta, sem uma entrada animada inteligente. Este arquivo existe para separar o inventario da correcao: nem todo item abaixo deve ser resolvido no mesmo patch de performance.

## Criterios

- `confirmed`: observado no Browser ou evidente no codigo renderizado.
- `suspected`: padrao de codigo sugere troca seca, ainda precisa de auditoria visual.
- `watch`: ja tem animacao, mas pode piscar em reload, troca de rota, fallback ou HMR.

## Itens

| Status | Prioridade | Area | Arquivo | Sintoma / evidencia | Proxima acao |
| --- | --- | --- | --- | --- | --- |
| confirmed | alta | Troca de rota | `src/App.tsx`, `src/index.css` | Loader "Carregando secao" podia aparecer como um painel recortado: o fallback real do `Suspense` ficava dentro do `main` com `min-height: calc(100svh - 140px)`, enquanto a bottom nav e o shell continuavam em outra camada. O gutter desktop fica isolado por breakpoint e nao explica o recorte mobile principal. | Corrigido neste passe: loader de rota virou camada `fixed inset-0` em `100svh`, com respiro de safe area/bottom nav. Revalidar em `/#/`, `/#/stats`, `/#/circle`, `/#/settings`. |
| confirmed | alta | Troca de rota / chunk curto | `src/App.tsx`, `src/components/Layout.tsx` | Mesmo com loader corrigido, `Home -> Ajustes` ainda podia revelar o spinner por menos de 260 ms enquanto o chunk resolvia, criando piscada visual. | Corrigido neste passe: `RouteLoader` so aparece apos 320 ms; preloads de rota saem do idle e viram tarefa de interacao nomeada. |
| confirmed | alta | Troca rapida de rota | `src/App.tsx`, `src/components/Layout.tsx` | Em clique muito rapido enquanto chunks ainda estavam carregando, o Browser mostrou hash novo antes da tela nova estar assentada. Na volta Stats -> Home, a medicao no Browser mostrou o cover com `390x754` dentro de viewport `390x844`, deixando uma faixa visual entre cover e bottom nav. | Corrigido neste passe com cover de intent em viewport inteiro (`inset-0`/`100svh`), mantendo a nav acima. Revalidar em navegacao rapida. |
| watch | alta | Troca de rota | `src/App.tsx` | Entrada da rota usa fade/translate simples no wrapper, mas conteudo interno de cada tela ainda pode montar inteiro quando o chunk ja esta em cache. | Criar padrao de entrada escalonada por tela, com delays curtos e transform-only. |
| confirmed | alta | Stats | `src/screens/StatsScreen.tsx` | Blocos de ranking, charts e Suspense skeletons podiam trocar skeleton por conteudo sem gate central completo; carrossel de insights usava `setTimeout` local. | Parcialmente corrigido neste passe: skeleton de ranking usa `Skeleton`/shimmer gated, linhas respeitam motion tier e insights rodam via `motionRuntime.scheduleTask`. Ainda auditar graficos e comparadores. |
| confirmed | alta | Circle | `src/screens/CircleScreen.tsx` | Trocas de aba (`now`, `arena`, `affinity`) ja tinham transicao de aba, mas headers de Arena/Afinidade e cards de afinidade ainda podiam montar secos dentro da aba. | Parcialmente corrigido neste passe com headers animados, cards de afinidade com `layout="position"` e motion runtime. Validar navegacao entre abas e retorno para Home. |
| watch | media | Home / LeoHeader | `src/components/home/LeoHeader.tsx` | O header tem coreografia propria, mas estados de loading/sync podem sobrepor conteudo e revelar o vinil/header de uma vez. A checagem funcional de fim da faixa ja foi roteada pelo `motionRuntime.scheduleTask`. | Auditar troca "sincronizando" -> musica pronta, preservando vinil/tonearm. |
| confirmed | media | Home / RankingSummary | `src/components/home/LeoHeader.tsx` | O fallback de repeats/ranking usava shimmer pequeno no slot do badge, criando aparencia de placeholder web antes do resumo real. | Corrigido neste passe: placeholder removido; repeats/ranking so montam quando ha dado real e entram por transform/opacity; botao Letra tambem ganha entrada curta para ocupar o slot sem parecer sobra. |
| confirmed | media | Texto em badges/pills | `src/components/home/LeoHeader.tsx`, `src/screens/SettingsScreen.tsx`, `src/screens/StatsScreen.tsx`, `src/components/home/ReplaySection.tsx`, `docs/motion-runtime-rules.md` | Microcopy em espacos estreitos era truncada ou espremida: `APPLE MUSIC` colava no horario, `Privacidade` nao cabia na nav de Settings, `Mais tocados`/`Semana` podiam passar do chip e `Arena do Grupo` cortava no card-resumo. | Corrigido neste passe: status da faixa quebra em segmentos com separador, labels compactos ganharam tracking responsivo, nav de Settings virou scroll horizontal com pills `shrink-0`, cards-resumo reservam largura minima e nomes longos truncam de forma controlada. |
| confirmed | media | Mini tray sync | `src/components/Layout.tsx` | A bandeja compacta/expandida ainda dependia de animacao direta de `width/height`, o que podia fazer a bolha parecer "medida" pelo layout em vez de expandir como superficie nativa. | Corrigido neste passe com palco central fixo e placa visual expandindo por `scaleX/scaleY`; revalidar com membros live reais. |
| confirmed | alta | Retorno quente para Home | `src/components/Layout.tsx`, `src/screens/HomeScreen.tsx` | No Browser, `Ajustes -> Home` pela bottom nav ainda podia mostrar `RouteIntentCover`/`Carregando secao` por ~120 ms mesmo com a Home ja aquecida. | Corrigido neste passe: o Layout considera Home quente por estado React, sessionStorage e `data-stats-lc-home-ready-ms`; retorno quente limpa o intent sem abrir loader. |
| confirmed | alta | LeoHeader / Letra | `src/components/Layout.tsx`, `src/components/home/LeoHeader.tsx` | Botao Letra do LeoHeader abria a folha visualmente, mas ainda montava o modal de stats offscreen e acordava o pipeline de stats da faixa. | Corrigido neste passe: Letra standalone nao renderiza `.bottom-track-stats-modal`, nao hidrata ranking/stats e fecha por uma folha propria. |
| confirmed | alta | Bottom sheet / fechamento | `src/components/Layout.tsx`, `src/index.css` | O modal da bolha e a folha de letra desmontavam cedo ou perdiam a saida fluida; overrides de `transform: none` podiam cortar a transicao. | Corrigido neste passe: removidos overrides de transform no estado final, fechamento usa safety tasks nomeadas e a folha de letra mantem estado explicito de closing antes de desmontar. |
| confirmed | alta | Vinil / tonearm manual | `src/components/home/VinylRecord.tsx`, `src/components/home/VinylTonearm.tsx` | Ao tirar ou colocar a agulha manualmente, a sequencia automatica podia puxar o tonearm de volta para repouso/tocando em vez de preservar o ponto solto. | Corrigido neste passe: o tonearm preserva `level` manual, ignora a automacao ate mudar a faixa e so alterna playback ao cruzar o limiar. |
| confirmed | media | Blur em superficies repetidas | `src/index.css`, `src/components/Layout.tsx` | Pills internas do mini tray e surfaces compartilhadas usavam `backdrop-filter: blur(42px)` em varios elementos simultaneos; modal frosted usava `72px`. Mesmo estatico, isso aumenta custo de composicao em mobile. | Corrigido neste passe: surfaces repetidas caem para `24px/165%`, modal frosted para `38px/170%`; overrides `balanced`/`conserve` continuam reduzindo mais quando o runtime detecta pressao. |
| confirmed | media | Atividade do circulo | `src/components/home/FriendActivityReel.tsx` | Mudanca de musica por amigo e reordenacao horizontal tinham animacao parcial, mas a capa entrava do lado oposto ao pedido e alguns initial/exit ainda rodavam mesmo em runtime conservador. | Corrigido neste passe com entrada da esquerda para a direita dentro do recorte, `layoutDependency` por faixa e gate por motion runtime. |
| confirmed | media | Settings | `src/screens/SettingsScreen.tsx`, `src/components/settings/SettingsUI.tsx` | Secoes de ajustes montavam estaticas depois da entrada da rota, dando sensacao de bloco inteiro aparecendo. | Corrigido neste passe com entrada escalonada por `SettingsGroup`, ligada ao motion runtime. Auditar entradas internas especificas depois. |
| confirmed | media | Engine loops | `src/components/shared/CommonUI.tsx`, `src/index.css` | Loops `Engine*` ambientais comecavam como visiveis ate o `IntersectionObserver` responder, podendo ativar animacoes fora de viewport por alguns frames. | Corrigido neste passe: loops ambient comecam pausados e CSS corta `animation/will-change` quando `data-active=false`; validar contagem em `data-stats-lc-compositor-loops`. |
| confirmed | media | Telemetria do scheduler | `src/lib/motionRuntime.ts`, `src/components/home/LeoHeader.tsx`, `src/hooks/useAutoOrbitRotation.ts`, `src/screens/StatsScreen.tsx` | Contagens de tarefas e loops podiam permanecer acima de zero sem revelar se eram relogio funcional, checagem de playback, rotacao ambiental ou profundidade visivel. | Corrigido neste passe: tarefas longas e loops recebem `kind`, exposto em `data-stats-lc-motion-task-kinds`, `data-stats-lc-compositor-loop-kinds` e `getSchedulerStats()`. |
| confirmed | media | Engine timers | `src/components/home/LeoHeader.tsx` | Relogio de progresso da faixa usava `setInterval` proprio, fora do scheduler central, embora atualizasse DOM sem rerender amplo. | Corrigido neste passe com `motionRuntime.scheduleTask`; manter proximos timers quentes no mesmo runtime. |
| confirmed | media | Scheduler / polling | `src/screens/HomeScreen.tsx`, `src/App.tsx`, `src/lib/motionRuntime.ts` | O progresso visual de sincronizacao ainda tinha `setInterval` proprio; o polling live usava intervalo fixo, que podia acordar em aba oculta ou iniciar um novo ciclo antes do anterior encerrar. | Corrigido neste passe: progresso visual entrou no scheduler central; polling live virou timeout recursivo visibility-aware e sem sobreposicao. Tarefas funcionais de interacao agora concluem mesmo se a aba ficar oculta; apenas tarefas ambientais sao descartadas. |
| confirmed | alta | Timers visuais dispersos | `src/App.tsx`, `src/screens/HomeScreen.tsx`, `src/screens/SettingsScreen.tsx`, `src/components/Layout.tsx`, `src/components/shared/CommonUI.tsx`, `src/screens/StatsScreen.tsx`, `src/screens/RankingScreen.tsx`, `src/components/home/DiscreetFriendsRow.tsx` | Delays de splash, preloading, pulso de filtros, highlight de bolhas, dismiss de toast, fallback de imagem, medicao tardia de marquee e scroll programatico ainda usavam `setTimeout` local, mesmo com cleanup em parte deles. | Corrigido neste passe: delays visuais migrados para `motionRuntime.scheduleTask`; tarefas de UI aberta usam prioridade `interaction`, prefetch visual fechado usa `ambient` e todos os canceladores ficam em refs/cleanup. Timers restantes em `src/App.tsx`, stores e services foram classificados como polling/rede/I/O/safety. |
| confirmed | media | Store/cache timers | `src/store/useStatsStore.ts`, `src/components/home/ReplaySection.tsx` | Invalidação de caches expirados usava `setTimeout(..., 100)` só para adiar mutação; ReplaySection tinha fallback local de `requestIdleCallback`. | Corrigido neste passe: invalidação de cache virou microtask (`deferCacheInvalidation`) e o fallback do Replay usa `motionRuntime.scheduleTask(..., 'ambient')`. Timers restantes do store são deadlines de preparo visual, prefetch com abort e safety timers de rede. |
| confirmed | media | Filas de assets/paleta | `src/lib/assetRuntime.ts`, `src/lib/colorUtils.ts` | Fallbacks de `requestIdleCallback` para drenar imagem/paleta ainda usavam `setTimeout(..., 0)`, criando uma politica paralela para trabalho visual quando o browser nao oferece idle callback. | Corrigido neste passe: fallback das filas entra em `motionRuntime.scheduleTask(..., 'interaction')` para nao ser descartado no tier conservador; a propria fila ainda respeita visibilidade, concorrencia e prioridade dos jobs. Deadlines de carregamento de imagem continuam como timeout funcional. |
| confirmed | media | Acoes compartilhadas | `src/components/shared/ShareButton.tsx`, `src/components/shared/MusicCard.tsx`, `src/components/history/FriendHistoryCard.tsx`, `src/components/shared/SnapshotHistoryModal.tsx` | Botoes compartilhados ainda tinham `transition-all`; `ShareButton` usava timers locais para resetar estado apos compartilhar/erro. | Corrigido neste passe: reset do share usa `motionRuntime.scheduleTask`, transicoes sao explicitas e botoes de historico/musica evitam `transition-all`. |
| confirmed | media | Modais de historico | `src/components/modals/UserHistoryModal.tsx`, `src/components/modals/TrackHistoryModal.tsx`, `src/components/modals/UserAlbumHistoryModal.tsx`, `src/components/modals/TrackLeaderboardModal.tsx`, `src/components/modals/EntityStatsModal.tsx` | Havia `transition-all`, rows com entrada independente do motion tier, loops do cenario continuavam ativos ao fundo e containers misturavam `vh`/`dvh` com o shell mobile em `svh`. | Corrigido neste passe: entrada de modal/lista usa gate do motion runtime, shimmers manuais respeitam `data-active`, transicoes ficaram explicitas, alturas usam `svh` e o escopo modal pausa loops do fundo com suporte a entidade aninhada. Ainda auditar a troca interna de abas/letras. |
| confirmed | media | Comparadores | `src/components/stats/FriendsStatsComparer.tsx`, `src/components/stats/GlobalStatsComparer.tsx` | Superficies de selecao, abas e listas usavam `transition-all`; dropdowns/tab content animavam sem consultar o motion tier. | Parcialmente corrigido: `GlobalStatsComparer` sem `transition-all`; `FriendsStatsComparer` com listas internas ligadas ao runtime, mas atualmente nao e montado por nenhuma tela. O fluxo real do Ranking usa `GlobalStatsComparer`. |
| confirmed | media | Modal de usuario / batalha | `src/components/modals/UserModals.tsx`, `src/hooks/useModalMotionScope.ts` | `UserDetailModal` e `StatsBattleModal` usavam `vh`, `transition-all` e coreografias longas no tier conservador; rows virtualizadas reanimavam ao montar e loops do pódio/mini tray continuavam rodando atrás do modal. | Corrigido neste passe: viewport em `svh`, entradas ligadas ao runtime, delays encurtados, rows virtualizadas animam apenas a primeira janela e o escopo de modal pausa loops do fundo com contador para modais aninhados. Ainda auditar o blur estatico do avatar e `AlbumDetailModal`. |
| confirmed | media | Bolha inferior / letras | `src/components/Layout.tsx`, `src/hooks/useModalMotionScope.ts` | O modal da bolha inferior e a folha de letras eram portais internos do Layout; a letra usava `82dvh`, enquanto o cover de rota e os demais modais usam viewport estavel. Loops do cenario podiam continuar ativos sob a folha. | Corrigido neste passe: portal inteiro participa do escopo modal, animações internas continuam liberadas e a folha de letras usa `82svh`. Revalidar abertura, letra, action sheet e fechamento por gesto. |
| confirmed | media | Mini tray / hidratação interna | `src/components/Layout.tsx` | A bolha inferior ainda atrasava fetch de recentes, letras, ranking e stats com `setTimeout` local. Mesmo com cleanup, esses callbacks podiam concorrer com abertura/fechamento da superficie. | Corrigido neste passe: hidratação com modal aberto usa tarefas `interaction`; prefetch fechado usa tarefas `ambient`; fechamento atrasado e mask de scroll de letras sao cancelados por refs. Revalidar abrir/fechar bolha, letra e voltar para Home. |
| confirmed | media | Loaders de modais lazy | `src/components/shared/LazyModalFallback.tsx`, `src/screens/HomeScreen.tsx`, `src/screens/RankingScreen.tsx`, `src/screens/StatsScreen.tsx`, `src/screens/CircleScreen.tsx` | O fallback de `Suspense` aparecia como overlays diferentes antes do modal real, alguns secos e sem participar do orçamento global de loops. | Corrigido neste passe: fallback compartilhado em `100svh`, entrada curta em duas etapas, badge laranja e escopo modal que pausa o cenário enquanto o chunk abre. |
| confirmed | media | Seletor da Arena | `src/screens/RankingScreen.tsx` | O popover possuia entrada propria, mas a lista podia aparecer toda no mesmo frame em aparelhos lentos, dando efeito de bloco. | Corrigido neste passe: opcoes entram com stagger curto de 24 ms ate os primeiros itens, respeitando o motion runtime; botao ganhou `aria-label`/`aria-expanded`. Revalidar visualmente na Arena. |

## Notas de execucao

- Preferir `opacity`, `translate3d`, `scale` e `layout="position"` quando a lista realmente muda de ordem.
- Evitar animar `height`, `width`, blur pesado ou sombras em loop.
- Delays devem ser curtos e intencionais: o objetivo e ritmo premium, nao lentidao.

## Evidencia mobile atual

- Browser in-app em `390x844`: ciclo Home -> Stats -> Circulo -> Ajustes -> Home sem overflow horizontal, imagens quebradas ou warnings/errors.
- Browser in-app em `390x844`: `Ajustes -> Home` pela bottom nav revalidado sem `RouteIntentCover`, sem texto visivel "Carregando secao" aos 70/160/660 ms e sem overflow.
- Browser in-app em `390x844`: `Home -> Ajustes` apos loader atrasado: aos 80 ms nao ha `RouteLoader`/texto; aos 260 ms Settings ja esta montado, sem overflow e console limpo.
- Browser in-app em `390x844`: Letra do LeoHeader abre com `.bottom-track-lyrics-modal` e sem `.bottom-track-stats-modal`; fechamento remove a folha e preserva overflow `0`.
- Browser in-app em `390x844`: bolha inferior abre modal de stats sem overflow e o fechamento mantem o modal durante a saida antes do safety desmontar.
- Browser in-app em `390x844`: tonearm manual validado por DOM; ao tirar ficou estavel em `data-tonearm-level=0.181`, ao recolocar ficou estavel em `0.818`, sem snap apos 1,2 s.
- Browser in-app em `390x844`: foot sync compacto `70x32` centralizado; expandido `318x56`, sem barra gigante de blur e sem overflow.
- Browser in-app em `390x844`, apos migracao dos timers visuais: Home quente, Stats, Circulo, Ajustes e retorno para Home sem overflow horizontal, sem imagens quebradas e sem logs `warn`/`error`; loaders/cover medidos com `390x844` quando presentes.
- Retorno Ajustes -> Home pela bottom nav: sem loader recortado; troca quente passa por uma janela curta com a tela anterior ainda montada e depois Home entra sem overflow. Continuar observando visualmente se isso deve virar cover obrigatorio mesmo em transicoes muito rapidas.
- Retorno a Home: `4` loops CSS realmente ativos; nenhum elemento `data-active=false` manteve `animation-name`.
- Circulo: palco orbital com `11` loops compositor desejados quando visivel e `0` ao sair da viewport.
- Circulo fora da viewport: delta `0` de long tasks/LoAF em `8s` de idle e em scroll leve de ida/volta.
- Bolha inferior: superficie modal `390x844`, cenario com `0` loops ativos, `5` loops internos preservados; folha de letra termina exatamente em `844px`.
- Runtime de assets no passe: classe de memoria `standard` (`8 GB`), `19/180` imagens carregadas, filas de imagem/paleta em `0`.
